<%= form_with(model: cert_agency, local: true) do |form| %>
  <% if cert_agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cert_agency.errors.count, "error") %> prohibited this cert_agency from being saved:</h2>

      <ul>
      <% cert_agency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cert_agency %>
    <%= form.text_field :cert_agency %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
